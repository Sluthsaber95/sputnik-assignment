import React from "react";
import { storiesOf } from "@storybook/react";

import { NavigationTop } from "../components"

storiesOf("Top Navigation", module).add("to Storybook", () => (
  <NavigationTop />
));
